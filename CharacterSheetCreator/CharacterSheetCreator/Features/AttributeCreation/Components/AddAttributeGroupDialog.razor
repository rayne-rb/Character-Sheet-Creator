@using CharacterSheetCreator.Features.AttributeCreation.Services

<MudDialog>
    <TitleContent>
        <MudText>@MudDialog.Title</MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField T="string" Label="Group Name" Variant="Variant.Outlined" Value="GroupName" ValueChanged="GroupNameChanged"></MudTextField>
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Filled" Color="Color.Error" OnClick="Cancel">Cancel</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Success" OnClick="Add">Add</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] public required IMudDialogInstance MudDialog { get; set; }
    [Inject] public IAttributesService AttributesService { get; set; }

    public string? GroupName { get; set; } = string.Empty;

    public async Task GroupNameChanged(string groupName)
    {
        GroupName = groupName;
    }

    private async Task Add()
    {
        var result = await AttributesService.CreateAttributeGroup(GroupName);
        if(result.IsT1)
        {
            MudDialog.Close(DialogResult.Ok(false));
        }
        else if (!result.AsT0)
        {
            MudDialog.Close(DialogResult.Ok(false));
        }
        else
        {
            MudDialog.Close(DialogResult.Ok(true));
        }
    }

    void Cancel()
    {
        MudDialog.Cancel();
    }
    
}